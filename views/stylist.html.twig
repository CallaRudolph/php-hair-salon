<html>
    <head>
        <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css'>
        <title>Stylist Info</title>
     </head>
      <body>
        <div class='container'>
            <h2>{{ stylist.getName }}</h2>
            <div class='well'>
                {% if clients is not empty %}
                    <p>Here are {{ stylist.getName }}'s clients:</p>
                    <ul>
                        {% for client in clients %}
                            <li>{{ client.getClientName }}</li>
                            <ul class='list-unstyled'>
                              <li>Phone#: {{ client.getPhoneNumber }}</li>
                            </ul>
                        {% endfor %}
                {% else %}
                    <p> This stylist does not have any clients yet. No good!</p>
                    </ul>
                {% endif %}
            </div>
            <br>
            <div class='well'>
                <h4>Add a Client for {{ stylist.getName }}</h4>
                <form action='/clients' method='post'>
                    <input id="stylist_id" name="stylist_id" type="hidden" value="{{ stylist.getId() }}">
                    <label for='client_name'>Client Name:</label>
                    <input id='client_name' name='client_name' type='text'><br>
                    <label for='phone_number'>Client Phone Number:</label>
                    <input id='phone_number' name='phone_number' type='text'><br><br>

                    <button type='submit'>Add Client</button>
                </form><br>
            </div>
            <p><a href="/stylists/{{ stylist.getId }}/edit">Edit this stylist</a></p>
            <p><a href='/'>Go back to all stylists</a></p>
        </div>
      </body>
</html>
